(ns lineal.test.mit.hw5
  (:require [clojure.test :refer :all]
            [lineal.linear :refer :all]
            [lineal.numbers :as numbers]
            [lineal.test.vector-spaces :refer :all]
            [clojure.spec.alpha :as s]
            [clojure.spec.gen.alpha :as gen]))

(deftest "Homework 5"
  (testing "10.1"
    (testing "(a) A nonzero matrix whose eigenvectores are all 0"
      (is false))
    (testing "(b) Nilpotent matrix."
      (is false))
    (testing "(c) Jordan Blocks of Nilpotent matrix."
      (is false))
    (testing "(d) For an arbitrarily square matrix, what is the smallest...?"
      (is false)))
  (testing "11.1"
    (is false))
  (testing "11.3"
    (is false))
  (testing "11.4"
    (is false)))
